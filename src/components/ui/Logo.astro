<span class="logo" aria-label="Bajo Cero — Ir al inicio">
  <span class="logo__crest">
    <span class="logo__top">
      <span class="logo__line" aria-hidden="true"></span>
      <svg class="logo__snowflake" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <path d="M12 2v20M2 12h20M4.9 4.9l14.2 14.2M19.1 4.9L4.9 19.1"/>
        <path d="M12 2l-2 3.5M12 2l2 3.5M12 22l-2-3.5M12 22l2-3.5"/>
        <path d="M2 12l3.5-2M2 12l3.5 2M22 12l-3.5-2M22 12l-3.5 2"/>
        <path d="M4.9 4.9l3 2.5M4.9 4.9l2.5 3M19.1 19.1l-3-2.5M19.1 19.1l-2.5-3"/>
        <path d="M19.1 4.9l-2.5 3M19.1 4.9l-3 2.5M4.9 19.1l2.5-3M4.9 19.1l3-2.5"/>
      </svg>
      <span class="logo__line" aria-hidden="true"></span>
    </span>

    <h1 class="logo__main">
      <span class="logo__word">
        <span class="logo__letter logo__letter--large">B</span>
        <span class="logo__letter logo__letter--small">AJO</span>
      </span>
      <span class="logo__word-gap" aria-hidden="true"></span>
      <span class="logo__word">
        <span class="logo__letter logo__letter--large">C</span>
        <span class="logo__letter logo__letter--small">ERO</span>
      </span>
    </h1>

    <span class="logo__bottom">
      <span class="logo__line logo__line--faint" aria-hidden="true"></span>
      <span class="logo__sub">CONGELADOS</span>
      <span class="logo__line logo__line--faint" aria-hidden="true"></span>
    </span>
  </span>
</span>

<style>
  .logo {
    display: inline-flex;
    align-items: center;
    text-decoration: none;
    transition: opacity var(--transition-fast);
  }

  .logo:hover {
    opacity: 0.85;
  }

  .logo__crest {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 2px;
    user-select: none;
  }

  /* ── Top row: fine lines + delicate snowflake ── */
  .logo__top {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    width: 100%;
    max-width: 200px;
    margin-bottom: 0;
  }

  .logo__snowflake {
    width: 14px;
    height: 14px;
    flex-shrink: 0;
    color: #3B82F6;
    opacity: 0.85;
  }

  .logo__line {
    flex: 1;
    height: 1px;
    background-color: rgba(59, 130, 246, 0.4);
  }

  .logo__line--faint {
    flex: 1;
    height: 1px;
    background-color: rgba(59, 130, 246, 0.25);
  }

  /* ── Main text: BAJO CERO (protagonist) ── */
  .logo__main {
    display: flex;
    align-items: baseline;
    justify-content: center;
    gap: 8px;
    margin: 0;
    font-family: 'Playfair Display', 'Georgia', 'Times New Roman', serif;
    line-height: 1;
  }

  .logo__word {
    display: inline-flex;
    align-items: baseline;
  }

  .logo__word-gap {
    width: 0;
  }

  .logo__letter--large {
    font-size: 24px;
    font-weight: 700;
    letter-spacing: 0;
  }

  .logo__letter--small {
    font-size: 19px;
    font-weight: 600;
    letter-spacing: 0.08em;
  }

  /* ── Bottom row: CONGELADOS (subtle) ── */
  .logo__bottom {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    width: 100%;
    margin-top: 1px;
  }

  .logo__sub {
    font-family: var(--font-body);
    font-size: 9px;
    font-weight: 400;
    text-transform: uppercase;
    letter-spacing: 0.3em;
    white-space: nowrap;
    line-height: 1;
  }

  /* ── Light theme ── */
  :global(html[data-theme="light"]) .logo__letter--large,
  :global(html[data-theme="light"]) .logo__letter--small {
    color: #0B1B3D;
  }

  :global(html[data-theme="light"]) .logo__sub {
    color: #64748B;
  }

  :global(html[data-theme="light"]) .logo__line--faint {
    background-color: rgba(100, 116, 139, 0.3);
  }

  /* ── Dark theme with subtle neon ── */
  :global(html[data-theme="dark"]) .logo__letter--large,
  :global(html[data-theme="dark"]) .logo__letter--small {
    color: #ffffff;
    filter: drop-shadow(0 0 8px rgba(255, 255, 255, 0.15));
  }

  :global(html[data-theme="dark"]) .logo__sub {
    color: rgba(147, 197, 253, 0.7);
  }

  :global(html[data-theme="dark"]) .logo__snowflake {
    filter:
      drop-shadow(0 0 5px rgba(59, 130, 246, 0.5))
      drop-shadow(0 0 10px rgba(0, 229, 255, 0.15));
  }

  :global(html[data-theme="dark"]) .logo__line {
    background-color: rgba(147, 197, 253, 0.3);
    box-shadow:
      0 0 4px rgba(59, 130, 246, 0.4),
      0 0 8px rgba(0, 229, 255, 0.1);
  }

  :global(html[data-theme="dark"]) .logo__line--faint {
    background-color: rgba(147, 197, 253, 0.2);
    box-shadow:
      0 0 3px rgba(59, 130, 246, 0.2);
  }
</style>
