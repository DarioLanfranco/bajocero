---
import Icon from '../ui/Icon.astro';
---

<div class="about__metrics" data-reveal data-reveal-stagger="children" data-reveal-delay="250">
  <div class="about__metric">
    <div class="about__metric-icon" aria-hidden="true">
      <Icon name="layers" size={24} strokeWidth={1.5} />
    </div>
    <div class="about__metric-body">
      <span class="about__metric-value">98%</span>
      <span class="about__metric-label">Nutrientes preservados</span>
    </div>
  </div>

  <div class="about__metric-divider" aria-hidden="true"></div>

  <div class="about__metric">
    <div class="about__metric-icon" aria-hidden="true">
      <Icon name="clock" size={24} strokeWidth={1.5} />
    </div>
    <div class="about__metric-body">
      <span class="about__metric-value">&lt;20min</span>
      <span class="about__metric-label">Del freezer al plato</span>
    </div>
  </div>

  <div class="about__metric-divider" aria-hidden="true"></div>

  <div class="about__metric">
    <div class="about__metric-icon" aria-hidden="true">
      <Icon name="grid-plus" size={24} strokeWidth={1.5} />
    </div>
    <div class="about__metric-body">
      <span class="about__metric-value">0</span>
      <span class="about__metric-label">Conservantes añadidos</span>
    </div>
  </div>
</div>

<style>
  .about__metrics {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-md);
  }

  .about__metric {
    display: flex;
    align-items: center;
    gap: var(--space-md);
    padding: var(--space-md) var(--space-lg);
    border-radius: var(--radius-md);
    background-color: var(--color-surface);
  }

  .about__metric-divider {
    display: none;
  }

  .about__metric-icon {
    color: var(--color-brand);
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 44px;
    height: 44px;
    border-radius: var(--radius-md);
    background-color: rgba(var(--color-brand-rgb), 0.08);
  }

  .about__metric-body {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .about__metric-value {
    font-family: var(--font-heading);
    font-size: var(--font-size-xl);
    font-weight: 700;
    color: var(--color-text);
    line-height: 1.1;
  }

  .about__metric-label {
    font-size: var(--font-size-xs);
    color: var(--color-text-secondary);
    line-height: 1.4;
  }

  @media (min-width: 1024px) {
    .about__metrics {
      grid-template-columns: 1fr auto 1fr auto 1fr;
      align-items: center;
      gap: 0;
      background-color: var(--color-surface);
      border-radius: var(--radius-md);
      padding: var(--space-md) 0;
    }

    .about__metric {
      padding: 0 var(--space-lg);
      background: none;
      border-radius: 0;
    }

    .about__metric-divider {
      display: block;
      width: 1px;
      height: 40px;
      background: linear-gradient(
        to bottom,
        transparent 0%,
        rgba(var(--color-brand-rgb), 0.2) 50%,
        transparent 100%
      );
    }

    .about__metric-icon {
      width: 36px;
      height: 36px;
    }

    .about__metric-value {
      font-size: var(--font-size-lg);
    }
  }

  @media (max-width: 1023px) {
    .about__metric {
      min-width: 100%;
    }

    .about__metric-value {
      font-size: var(--font-size-xl);
    }
  }

  @media (max-width: 767px) {
    .about__metrics {
      flex-direction: column;
    }
  }
</style>
